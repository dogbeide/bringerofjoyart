import { Cabin_Sketch, Amatic_SC } from "next/font/google";

export const cabin_sketch = Cabin_Sketch({ weight : "400", subsets: ['latin'] });
export const amatic_sc = Amatic_SC({ weight: "400", subsets: ['latin'] });
